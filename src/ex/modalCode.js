
    // const [modalVisible, setModalVisible] = useState(false);




{/* <View>
            <Modal
            animationType='slide'
            transparent={true}
            presentationStyle="overFullScreen"
            visible={modalVisible}
            onRequestClose={() => {
                Alert.alert("Modal Closed");
            setModalVisible(!modalVisible);
            }}>
                <View style={{ flex: 1, justifyContent: 'center', marginTop: 22}}>
                <View style={{margin: 20, backgroundColor: 'white', height: 550, width: 330, borderRadius: 20, padding: 35, alignItems: 'center', shadowColor: '#000', shadowOffset: { 
                    width: 0, height: 2
                }, shadowOpacity: 0.25, shadowRadius: 4, elevation: 20}}>
                    <Text>First Modal</Text>
                    <TextInput style={{ marginTop: 0, height: 250, width: 280, borderColor: 'black', borderWidth: .5, borderRadius: 3, fontSize: 15, padding: 10, paddingTop: 10, paddingBottom: 10}}
                        multiline></TextInput>
                    <Button onPress={() => setModalVisible(!modalVisible)}>Close Modal</Button>
                </View>
                </View>
            </Modal>
            <Button style={{marginTop: 50}} onPress={() => setModalVisible(true)}>Open Modal</Button>
            </View> */}